valueToProperty = (value, prop) ->
  value += ''
  value = value.split(':')
  time = 0

  for i in [value.length - 1, 0]
    time = time * 60 + Number(value[i])
  time


timeToSeconds = (value = '0:00:00', prop) ->
	value = value.split(':')

	for


console.log valueToProperty('3:23:11')
