diff --git a/lib/ORM.js b/lib/ORM.js
index 85e25bd..9b82d70 100644
--- a/lib/ORM.js
+++ b/lib/ORM.js
@@ -174,6 +174,17 @@ function ORM(driver_name, driver, settings) {
 
 util.inherits(ORM, events.EventEmitter);
 
+ORM.prototype.createTransaction = function () {
+	new ORM(this.driver_name, driver, settings);
+	// Need to clone entire ORM
+}
+ORM.prototype.commit = function () {
+
+}
+ORM.prototype.rollback = function () {
+
+}
+
 ORM.prototype.use = function (plugin_const, opts) {
 	if (typeof plugin_const == "string") {
 		try {
